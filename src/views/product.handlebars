<table class="table">
   <thead>
      <tr>
         <th>Title</th>
         <th>Description</th>
         <th>Price</th>
         <th>Category</th>
      </tr>
   </thead>
   <tbody>
      {{#each products}}
      <tr>
         <td>{{title}}</td>
         <td>{{description}}</td>
         <td>{{price}}</td>
         <td>{{category}}</td>
      </tr>
      {{/each}}
   </tbody>
</table>
<p>Hace click <a href="/products/pushProduct">acá</a> para añadir un nuevo producto</p>